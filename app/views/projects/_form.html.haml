= simple_form_for(@project, html: { method: :post }) do |f|
  .col-12
    .row
      = f.input :name, wrapper: :md8
      = f.input :key, wrapper: :md4
    .text-right
      = f.submit i18n('submit'), class: 'btn btn-primary'
