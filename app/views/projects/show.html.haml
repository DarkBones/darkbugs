-# = render partial: 'layouts/header', locals: { icon_class: 'fas fa-lightbulb', title: @project.name, title_sub: i18n('title_sub', name: @project.name) }
- content_for :page_title do
  = @project.name

#project-items-field
  #project-items-container
    = react_component('BoardsApp/BoardsApp', @props, { prerender: false })

-##project-items-app
-#  %h1
-#    Issues
-#  #columns
-#    - ['Open', 'In Progress', 'Done', 'Released', 'Archived'].each do |name|
-#      .column.rounded.p-2.pb-5
-#        %h4.column-title
-#          = name
-#        - ['one', 'two', 'three', 'four', 'five', 'six'].each do |card|
-#          .item-card.mb-3.p-2.rounded
-#            .font-weight-bold
-#              = card
